---
trigger: always_on
---

# What to do when I ask you to add apps to list
- I will paste the command to install it in scoop such as `scoop install games/steam`
- You will add the app `id` (e.g. `games/steam`) to the `applications.json` file in `/windows/configs`. Make sure you use the right json format in the file. 
 